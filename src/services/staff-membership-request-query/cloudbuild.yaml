steps:
  - name: "node:10.16.3"
    id: install
    entrypoint: npm
    args: ["install"]
    dir: src/services/staff-membership-request-query

  - name: "gcr.io/cloud-builders/docker"
    id: build
    waitFor:
      - install
    args:
      - build
      - --build-arg
      - service=staff-membership-request-query
      - -t
      - gcr.io/sweng-581-capstone/staff-membership-request-query:0.0.1
      - .

  - name: "gcr.io/cloud-builders/docker"
    id: push
    waitFor:
      - build
    args:
      [
        "push",
        "gcr.io/sweng-581-capstone/staff-membership-request-query:0.0.1",
      ]

  - name: "gcr.io/cloud-builders/gcloud"
    waitFor:
      - push
    args:
      - beta
      - run
      - deploy
      - staff-membership-request-query
      - --image=gcr.io/sweng-581-capstone/staff-membership-request-query:0.0.1
      - --region=us-central1
      - --platform=managed

images:
  - "gcr.io/sweng-581-capstone/staff-membership-request-query:0.0.1"
